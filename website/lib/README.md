# lib

Integrations and configured clients for external services — e.g. the Resend/Nodemailer email client for the contact form (12_FRONTEND_ARCHITECTURE.md), analytics init, or a future Prisma client. Anything that wraps a third-party SDK or reads an environment variable belongs here, not in `utils/`.

Empty for now — nothing external is wired up yet.
