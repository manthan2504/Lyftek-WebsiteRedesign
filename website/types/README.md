# types

Shared TypeScript types/interfaces grouped by domain (`service.ts`, `navigation.ts`, `contact.ts`, ...), not by component. Avoid `any`; prefer interfaces for object contracts per 17_CODING_STANDARDS.md.
