# components/sections

Page-specific composed sections built from `components/ui` primitives: Hero, ServicesGrid, WhyChooseUs, QuickContact, CTASection, etc.

One section = one responsibility = one file. If a section pattern repeats across pages (e.g. a generic "Section Header"), promote it to `components/ui` instead of duplicating.
