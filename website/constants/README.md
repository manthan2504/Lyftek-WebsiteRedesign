# constants

Static, non-secret project data: nav links, service catalog entries, site metadata defaults, social links. If it will never change at runtime and isn't a design token (those live in app/globals.css), it belongs here.
