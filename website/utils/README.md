# utils

Pure, stateless helper functions with no side effects and no knowledge of external services (`cn.ts`, `formatDate.ts`, `generateMetadata.ts`, ...). If it doesn't need an API key, a client instance, or config, it belongs here rather than in `lib/`.
